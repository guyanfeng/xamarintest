<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Test.BindingDemoPage"
             x:Name="page">
  <StackLayout BindingContext="{x:Reference page}" Orientation="Horizontal">
    <!--<Label BindingContext="{x:Reference slider}"
           Text="{Binding Path=Value,StringFormat='The Slider Value is:{0:F3}'}"
           Opacity="{Binding Path=Value}"
           VerticalOptions="CenterAndExpand">
    </Label>
    <Slider x:Name="slider"
            VerticalOptions="CenterAndExpand">
    </Slider>-->
    <Label Text="{Binding Path=Width}"></Label>
    <Label Text=" &#x00D7; "></Label>
    <Label Text="{Binding Path=Height}"></Label>
  </StackLayout>
</ContentPage>